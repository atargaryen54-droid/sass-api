initiating celery worker
    - celery -A app.core.celery_app.celery_app worker --loglevel=info

api_key for 
    -  sk_live_IkYVJP_DrXcrxKCHv1ayQ3BNAXZqwDRSJhL-61n_1Gw client 3(youtube)
    -  sk_live_ZlnbOpoQFjDie8MVp2WrUHC06FqrDwxHBtlW5ZRV0sI client 4(meta)


strpe cli command: stripe listen --forward-to localhost:8000/webhooks/stripe

Anthropic user:
                "email": "david@burroughs.com",
                "password": "chaltubegna"


run production containers:
        docker compose \
        -p saas-prod \
        --env-file .env.production \
        -f docker-compose.prod.yml \
        up -d \
        --pull never


run my prod database in docker:
    - docker exec -it -e PGPASSWORD=saas_production_password saas_db psql -U saas_user -d saas_db


want to know alembic's head migration
    - docker compose \
  -p saas-prod \
  --env-file .env.production \
  -f docker-compose.prod.yml \
  exec api alembic current

want to know image's head
 - change last bit to "exec api alembic heads"



our deployment triggers can be explained by:
    - "Pull requests run CI validation, while production deployment is triggered only after
       code reaches the protected main branch. The deployment uses an immutable container image,
       applies pending Alembic migrations, starts the application services, and verifies the 
       deployment through a health check."

CI and CD difference beatifully explained

        CI answers:

        "Is this code good enough to build?"

        Tests + Docker build.

        CD answers:

        "Did the new version actually become healthy?"